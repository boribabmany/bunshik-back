<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bunshik.kiosk.mapper.OrderMapper">

    <insert id="insertOrder">
        INSERT INTO orders
        (order_number, order_type, total_price)
        VALUES
        ('0', #{orderType}, #{totalPrice})
    </insert>

    <select id="getLastOrderId" resultType="int">
        SELECT LAST_INSERT_ID()
    </select>

    <update id="updateOrderNumber">
        UPDATE orders
        SET order_number = #{orderNumber}
        WHERE order_id = #{orderId}
    </update>

    <insert id="insertOrderItem">
        INSERT INTO order_items
        (order_id, menu_id, quantity, price_at_order)
        VALUES
        (#{orderId}, #{menuId}, #{quantity}, #{price})
    </insert>

    <select id="getLastOrderItemId" resultType="int">
        SELECT LAST_INSERT_ID()
    </select>

    <insert id="insertOrderItemOption">
        INSERT INTO order_item_options
        (order_item_id, option_id)
        VALUES
        (#{orderItemId}, #{optionId})
    </insert>

    <select id="getMenuPrice" resultType="int">
        SELECT price FROM menus WHERE menu_id = #{menuId}
    </select>

    <select id="getOptionPrice" resultType="int">
        SELECT option_price FROM options WHERE option_id = #{optionId}
    </select>

    <select id="isMenuOrderable" resultType="boolean">
        SELECT
        (is_visible = TRUE AND is_available = TRUE
        AND NOT EXISTS (
        SELECT 1 FROM menu_set ms JOIN menus cm ON ms.component_menu_id = cm.menu_id
        WHERE ms.set_menu_id = #{menuId} AND ms.select_group IS NULL
        AND (cm.is_available = FALSE OR cm.is_visible = FALSE)
        )
        AND NOT EXISTS (
        SELECT 1 FROM (
        SELECT ms.select_group,
        SUM(CASE WHEN cm.is_available = TRUE AND cm.is_visible = TRUE THEN 1 ELSE 0 END) AS avail_cnt
        FROM menu_set ms JOIN menus cm ON ms.component_menu_id = cm.menu_id
        WHERE ms.set_menu_id = #{menuId} AND ms.select_group IS NOT NULL
        GROUP BY ms.select_group
        ) grp WHERE grp.avail_cnt = 0
        ))
        FROM menus
        WHERE menu_id = #{menuId}
    </select>

    <select id="isOptionOrderable" resultType="boolean">
        SELECT (is_visible = TRUE AND option_is_available = TRUE)
        FROM options WHERE option_id = #{optionId}
    </select>

    <!-- 취소용: 현재 주문 상태 조회 -->
    <select id="getOrderStatus" resultType="String">
        SELECT order_status FROM orders WHERE order_id = #{orderId}
    </select>

    <!-- 주문 취소 (결제대기 상태인 주문만 서비스 레이어에서 검증 후 호출) -->
    <update id="cancelOrder">
        UPDATE orders
        SET order_status = '취소'
        WHERE order_id = #{orderId}
    </update>

<<<<<<< HEAD
    <!-- 세트(고정형/선택형 공통) 구성 후보 전체 조회 -->
    <select id="getSetComponentInfo" resultType="com.bunshik.kiosk.mapper.SetGroupInfo">
=======
>>>>>>> bff957e647d0cd0e443534b120b5c8bb549837dd
        SELECT ms.select_group AS selectGroup, ms.component_menu_id AS componentMenuId,
        cm.menu_name AS componentMenuName,
        ms.group_max_select AS groupMaxSelect, ms.extra_price AS extraPrice,
        (cm.is_available = TRUE AND cm.is_visible = TRUE) AS isAvailable
        FROM menu_set ms
        JOIN menus cm ON ms.component_menu_id = cm.menu_id
        WHERE ms.set_menu_id = #{setMenuId}
    </select>

    <insert id="insertOrderItemSetComponent">
        INSERT INTO order_item_set_components (order_item_id, component_menu_id, component_menu_name)
        VALUES (#{orderItemId}, #{componentMenuId},
        (SELECT menu_name FROM menus WHERE menu_id = #{componentMenuId}))
    </insert>

</mapper>